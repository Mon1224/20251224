# Tested versions for the AI Speech Trainer project (Windows / Python 3.11).
# Install with:  pip install -r requirements.txt
#
# Notes:
# - agno 2.x API is used (RunOutput, output_schema, enable_agentic_state, ...).
# - mediapipe is pinned to 0.10.33: newer wheels removed the legacy
#   `mp.solutions` API and older ones are incompatible with tensorflow 2.20.
# - protobuf is pinned explicitly: tensorflow 2.20 needs >=5.28, streamlit needs
#   <7, and mediapipe 0.10.33 is compatible with that range.

streamlit==1.53.1
pandas==2.3.3
plotly==6.5.2
opencv-python==4.13.0.90
tf-keras==2.20.1
deepface==0.0.98
mediapipe==0.10.33
agno==2.4.5
openai==2.15.0
requests==2.32.5
librosa==0.11.0
python-dotenv==1.2.1
moviepy==2.2.1
faster-whisper==1.2.1
fastapi==0.128.0
uvicorn==0.40.0
numpy==2.3.5
tensorflow==2.20.0
protobuf==6.33.6
